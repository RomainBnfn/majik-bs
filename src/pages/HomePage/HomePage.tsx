import "./HomePage.scss";
import { Link } from "react-router";

const HomePage = () => {
    return (
        <>
            :)
            <Link to={"/decks"}>Decks</Link>
        </>
    );
};

export default HomePage;
